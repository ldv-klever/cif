query: expand($) {
    $fprintf<"work/info.txt","%s %d %s %s %d\n",$expansion_path,$expansion_line,$macro_name,$path,$line>
}
query: expand($(..)) {
    $fprintf<"work/info.txt","%s %d %s %s %d\n",$expansion_path,$expansion_line,$macro_name,$path,$line>
}
